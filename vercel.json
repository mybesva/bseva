{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd bseva-export && NODE_ENV=production VITE_API_URL= pnpm run build:web",
  "outputDirectory": "bseva-export/dist/public",
  "installCommand": "cd bseva-export && corepack enable && pnpm install --frozen-lockfile && pip install -r ../requirements.txt",
  "functions": {
    "api/index.py": {
      "maxDuration": 30,
      "memory": 1024,
      "includeFiles": "backend/**"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
