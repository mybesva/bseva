{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd bseva-export && npm install && NODE_ENV=production VITE_API_URL= npm run build:web",
  "outputDirectory": "bseva-export/dist/public",
  "installCommand": "npm install --prefix bseva-export && pip install -r requirements.txt",
  "functions": {
    "api/index.py": {
      "maxDuration": 30,
      "memory": 1024,
      "includeFiles": "backend/**"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
