{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "regions": ["bom1"],
  "buildCommand": "cd bseva-export && NODE_ENV=production VITE_API_URL= VITE_ADMIN_PATH=${VITE_ADMIN_PATH:-/bseva-ops-m8k4q} pnpm run build:web",
  "outputDirectory": "bseva-export/dist/public",
  "installCommand": "cd bseva-export && corepack enable && pnpm install --frozen-lockfile",
  "functions": {
    "api/index.py": {
      "maxDuration": 30,
      "includeFiles": "backend/**"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
